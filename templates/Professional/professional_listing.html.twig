{% extends 'layouts/arqonz_layout.html.twig' %}
{% form_theme form 'bootstrap_5_layout_custom.html.twig' %}

{% block content %}
<script src="https://code.jquery.com/jquery-3.6.3.min.js"></script>
<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
<script src="https://cdnjs.cloudflare.com/ajax/libs/select2/4.0.8/js/select2.min.js" defer></script>


<div class="ArchitectListingcontentSection">
    {% if customertype == 'Contractor'%}
        <h1>Contractors for you</h1>
    {% endif %}
    {% if customertype == 'Designer'%}
        <h1>Designers for you</h1>
    {% endif %}
    {% if customertype == 'Architect'%}
        <h1>Architects for you</h1>
    {% endif %}
    {% if customertype == 'Builder'%}
        <h1>Builders for you</h1>
    {% endif %}
    {% if customertype == 'Manufacturer'%}
        <h1>Manufacturers for you</h1>
    {% endif %}
    {% if customertype == 'Engineer'%}
        <h1>Engineers for you</h1>
    {% endif %}
    {% if customertype == 'Retailer'%}
        <h1>Retailers for you</h1>
    {% endif %}
    {% if customertype == 'Dstributor'%}
        <h1>Distributors for you</h1>
    {% endif %}

    {% if filterform != '0' %}
        <div class="FilterForm">
            <div class="CityField">
                {{ form_start(form) }}
                {{ form_row(form.FilterCity, {'attr': {'class': 'js-select-cities', 'id':'cityfilterinput', 'value': 'FilterCity'}}) }}
            </div>
            <div class="SubmitButton">
                {{ form_row(form._submit, {'attr': {'class': 'btn btn-lg btn-block btn-success'}}) }}
                
            </div>
        </div>

        {{ form(form) }}
       
    {% endif %}
</div>
<div class="ArchitectListingSection">
    <div class="architect-list">
        
        {% for ProProfile in ProProfiles %}
            {% include '/Professional/ProProfile_listing_card.html.twig' with {'ProProfile': ProProfile, 'listingtype': customertype} %}
        {% endfor %}

    </div>
    <div class="pagination profilepagination">
        {% include 'includes/projectpagination.html.twig' with {'paginationVariables': paginationVariables} %}
    </div>
</div>





{# <script type="text/javascript">
$(document).ready(function() {
  $(".js-select-cities").select2();
});
</script> #}
{% endblock %}